# TenDispatch 产品与玩法基线

## 产品定位

TenDispatch 是一款面向 OpenTenBase 科普与竞技场景的 90 秒数据调度小游戏。玩家扮演数据物流中心的首席调度官，连续处理 14 波数据任务，在 DN 负载、查询效率、跨节点搬运和资源成本之间做取舍。

## 模式

### 新手教学

教学时长约 4～6 分钟，不参与排行榜，不计时、不失败，允许每波无限重试。流程拆成 11 个连续节点：玩家身份与游戏目标、CN / DN 数据链路、Shard / Replication、分片与查询路径、四项评分与三类决策参数、3 组“选择—结果—重试”训练，以及 Ranked 的状态继承、预测、撤回、Combo 和超时规则。三波训练依次覆盖用户活动记录、小型公共目录和海量业务日志；新手页面不展示极速模式的死亡条件。核心概念仍然是：信息用于判断而不是直接标出答案，小型高频公共数据可以 Replication，大型业务数据不应随意复制。

### Ranked 极速调度

固定 90 秒、14 波、同一 Daily Seed。系统状态不在波次之间重置：DN 当前负载、Replication 状态、资源占用和部分查询压力都会继承。系统从波次库中按 Daily Seed 无放回抽取，并固定为 3 个入门波、8 个中段波、3 个 Final Rush 波，保证每局内容有变化但难度比例稳定。前 3 波让分片关系明显，中间 8 波逐步加入查询、复制与资源成本，最后 3 波进入 Final Rush。

## 实时指标

- DN 负载：0～59% 稳定，60～79% 较高，80～94% 危险，≥95% 过载。
- 查询效率：用平均查询触达节点数表达，最佳约为 1.0。
- 跨节点搬运：0～100%，数据移动越多，成本越高。
- 资源占用：由复制、同步和持续写入累积，随状态继承。

## 单波循环

每波的任务卡只有三行：① 分布（规模 1～5 格、是否有集中风险）、② 查询方式（按编号 / 按地区汇总 / 按时间段 / 全局读取，以及是否为公共数据）、③ 本波权重（查询、写入、读取哪一项更重）。第三行是这一波唯一的优先级声明：它保证前两行不会各自指向不同的动作。按钮固定成物流动作，OpenTenBase 术语只在按钮第二行与结算的「本波对应」出现。每波还带一个查询开销系数：查询要触达几个 DN，这次查询就压在这几个 DN 上，所以对不上分流键的动作在节点数字上同样更差，而不是只在评分里被扣分。按钮数字、预测面板与评分共用 `src/game/loadModel.ts` 的同一份模型。玩家有 20 秒决策时间；超时不 Game Over，系统使用默认动作维持模拟线路，但该波总分为 0。

单波满分 100：

| 项目 | 分值 |
| --- | ---: |
| 节点负载均衡 | 40 |
| 查询效率 | 30 |
| 资源 / 复制成本 | 20 |
| 决策速度 | 10 |

评级为 PERFECT（90～100）、GOOD（75～89）、NORMAL（50～74）或 POOR（＜50）。连续 PERFECT / GOOD 建立 Combo，倍率为 1.00、1.10、1.20、1.30，连续 5 波及以上为 1.50；POOR 清零。

## 辅助与容错

每局提供预测 ×2 和撤回 ×1。预测用「仓会怎样」说话：哪个 DN 会先红、找人要问几个仓、会不会多占两份货位，分别对应均衡 40、查询 30、成本 20，该波最高评级封顶 GOOD。撤回最近一次决策，当前局扣 50 分并清零 Combo；重新决策时替换被撤回波次的结果。

## Final Rush

Wave 12 是大型业务数据进入，Wave 13 是高频查询突然增加，Wave 14 是公共数据访问激增。Final Rush 不引入新机制，只提高决策密度。GTM 自动启动事务协调动画，不要求玩家额外操作。

## 排行榜与结算

排序优先级为总分、PERFECT 数量、最大 Combo、完成时间、平均决策速度。同一个呼号在榜上只占一行，取这名玩家最好的一局（忽略首尾空格与大小写），重复成绩不重复上榜；结算里的排名、个人最佳和距 TOP 10 都按这一局计算。结算必须明确展示本局分、个人最佳、排名、距 TOP 10、最大 Combo、PERFECT 数量以及最多失分波次，并对照官方用法说明表该怎么放（分片表 / 复制表 / 触达节点代价）；记忆点是称号和官方用法对照，排行榜仍然只比分数。

第一版使用本机 LocalRepository，不虚构线上玩家和全球榜；Daily Seed、排序键和记录结构为未来接入真实排行榜保留接口。

## 教学与视觉原则

先让玩家看到数据如何经过 CN、进入 DN、产生倾斜或跨节点搬运，再命名 Shard、Replication 和 GTM。关键状态必须同时用文字、数值和颜色表达，并支持键盘焦点与减少动效偏好。主场景保持 1920×1080 逻辑画布等比缩放。
